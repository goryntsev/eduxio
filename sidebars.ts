import type {SidebarsConfig} from '@docusaurus/plugin-content-docs';

const sidebars: SidebarsConfig = {
  monkeysisyphusSidebar: ['monkeysisyphus/monkey', 'monkeysisyphus/monkeyjob', 'monkeysisyphus/sisyphus', 'monkeysisyphus/sisyphusmyth'],
};

export default sidebars;
